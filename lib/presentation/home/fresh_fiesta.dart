import 'package:flutter/cupertino.dart';

class FreshFiesta extends StatelessWidget {
  const FreshFiesta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Zepto"),
    );
  }
}